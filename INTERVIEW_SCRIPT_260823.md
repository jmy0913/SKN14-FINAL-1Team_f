# CodeNova 면접 대본 — 공고 맞춤판 (2026-08-23)

> **대상 공고 핵심 키워드**: Agent Workflow / LangGraph / 프롬프트 엔지니어링 / API·TOOL 연동 / 백엔드 개발 / 테스트·품질관리
>
> 이 문서는 **이번 공고에 맞춰 새로 쓰거나 교체할 부분만** 담았습니다.
> - 기본 대본(프로젝트 소개 30초·1분, LangGraph 상세, 멀티모달 등): `INTERVIEW_SCRIPT.md`
> - 상세 Q&A 41문항: `INTERVIEW_QA.md`
> - 수치 원본: `README.md` 상단 포트폴리오 섹션

---

## 목차

- [1. 공고 항목 ↔ 내 근거 매핑 (면접 직전 5분 훑기)](#1)
- [2. ★ "가장 어려웠던 부분" — 2축 대본](#2)
- [3. ★ "테스트 및 품질관리" 답변 — 2층 구조](#3)
- [4. 프롬프트 엔지니어링 보강 (기존 3-H에 넷째 사례 추가)](#4)
- [5. 프로젝트 소개 보강 2줄](#5)
- [6. 이번 공고용 전달 원칙](#6)
- [7. 면접 전 반드시 확인할 것](#7)

---

<a id="1"></a>
## 1. 공고 항목 ↔ 내 근거 매핑

| 공고 핵심 | 강도 | 내 근거 | 대표 수치 |
|---|:---:|---|---|
| **Agent Workflow** | 강 | 계획 → 툴 실행 → 관찰 → **자기 평가 → 재시도** 루프. 자율성(태그 선택)과 통제(권한·재시도 제한)를 분리 | 재시도 1회 제한 |
| **LangGraph** | 강 | 워크플로우 직접 설계, 노드 분리, MemorySaver 상태 관리 | 최근 4개 메시지 유지 |
| **프롬프트 엔지니어링** | 중 | 노드별 프롬프트 분리, temperature=0, JSON 모드, 회피성 답변 감지, 오타 교정, 툴 시그니처 주입 | — |
| **API 및 TOOL 연동** | 강 | 구글 API 11개 태그 자동 선택 툴 호출, Whisper STT, GPT-4o 비전, S3, Django↔FastAPI(vLLM) | 11개 태그 |
| **백엔드 개발** | 중 | Django + FastAPI 이중 구조, LangGraph를 `apichat/utils` 모듈로 통합, GPU 서빙 계층 분리 | — |
| **테스트 및 품질관리** | **강** | 3중 평가 체계(RAGAS / 자체 정량 / TOOL CALL 자동 채점) + 버전별 A/B 6단계 + Top-K 최적화 | 0.6750 → 0.9250 |

> **전략**: 아래 2-B(축 1) 하나가 공고 6개 중 **4개**(Agent Workflow · LangGraph · 프롬프트 · 품질관리)를 동시에 커버합니다. 그래서 LangGraph를 먼저 꺼냅니다.

---

<a id="2"></a>
## 2. ★ "가장 어려웠던 부분" — 2축 대본

**전체 약 2분 30초.** 면접관 반응이 미지근하면 2-F로 끊습니다.

### 2-A. 오프닝 (20초)

> "두 가지가 있었는데, 사실 **뿌리가 같은 문제**였습니다.
> **'모델이 언제 툴을 부를지, 그리고 결과가 나쁘면 어떻게 되돌릴지'** 인데요.
> 근데 **제어권이 모델 바깥에 있느냐 안에 있느냐**에 따라 푸는 방법이 완전히 달랐습니다. API 챗봇은 GPT-4o라 가중치를 못 건드리고, 사내 챗봇은 제가 직접 파인튜닝한 모델이었거든요. 순서대로 말씀드릴게요."

### 2-B. 축 1 — LangGraph 자가 교정 루프 (70초)

> "먼저 API 챗봇입니다. 처음엔 구조가 단순했어요. 질문이 들어오면 검색하고, 답변 만들고 끝. 그런데 문제가, **답변이 나빠도 시스템이 그걸 모른다**는 거였습니다. 검색이 엉뚱하게 됐는데도 그 문서로 그냥 답을 만들어서 내보내는 거죠.
>
> 그래서 답변 생성 뒤에 **품질 평가 노드**를 붙였습니다. LLM이 자기 답변을 good이냐 bad냐 판정하고, bad면 다시 검색하게요. 여기서 첫 번째 시도가 **'그럼 Top-K를 늘려서 더 많이 가져오자'** 였는데, **이게 오히려 성능을 떨어뜨렸습니다.** Context Recall이 단순 병합 버전의 0.8000에서 **0.6750까지 내려갔어요.**
>
> 원인을 뜯어보니, 검색을 더 많이 한 게 아니라 **같은 잘못된 쿼리로 더 깊게 판 것**이었습니다. 노이즈만 늘어난 거죠. 그래서 방향을 바꿨습니다. **양이 아니라 쿼리 자체를 바꿔야 한다**고 판단하고 HyDE를 적용했어요. 질문에 대한 가상의 답변을 먼저 만들고, 그 가상 답변으로 다시 검색하는 방식입니다. 이걸 넣으니까 Faithfulness가 **0.68에서 0.88로** 올랐습니다.
>
> 마지막으로 구글 API 원문이 영어라 한국어 쿼리가 잘 안 걸리는 문제가 있어서 **한국어와 영어 쿼리를 같이 생성**하도록 했고, 최종적으로 **Context Recall 0.9250, Faithfulness 0.8521**까지 나왔습니다.
>
> 물론 이게 무한 루프가 되면 안 되니까 **재시도는 최대 1회로 제한**했고, Top-K도 처음부터 크게 가는 게 아니라 **첫 검색은 5와 20, 재검색 때만 15와 30**으로 올리는 구조로 만들었습니다."

**여기서 흘려야 할 키워드**: 자기 평가 / 재시도 / 노드 분리 / 실패 → 원인 분석 → 방향 전환

### 2-C. 브릿지 (10초) — *절대 생략 금지*

> "그런데 이건 **GPT-4o라서** 이렇게 푼 거예요. 가중치를 못 건드리니까 **모델 바깥에서, 그래프 구조랑 프롬프트로** 제어한 거죠. 사내 모델은 상황이 달랐습니다."

### 2-D. 축 2 — 툴 호출 정책을 데이터로 설계 (70초)

> "사내 챗봇은 Qwen3-8B를 직접 파인튜닝했는데요, **먼저 솔직하게 말씀드리면 파인튜닝 자체가 처음이라 학습 스크립트는 멘토님 도움을 받았습니다.** 대신 제가 온전히 설계한 건 학습 데이터였고, 사실 성능을 가른 것도 그쪽이었습니다.
>
> 여기선 툴을 언제 부를지를 **모델 안에 심을 수** 있었는데, 이게 훨씬 어려웠어요.
> 툴을 **부르는 법**을 가르치는 건 예제 몇 개면 됩니다. 어려운 건 **안 부르는 법**이에요. 툴 호출 예시만 학습시키면 '안녕하세요'에도 문서 검색을 하고, 반대로 일상 대화만 늘리면 정작 업무 질문에 검색을 안 합니다.
>
> 게다가 저희는 멀티턴이라 단일 턴처럼 규칙으로 못 풉니다. 앞에서 이미 검색한 내용을 되묻는 후속 질문도 있고, 일상 대화하다 갑자기 업무 질문이 들어오기도 하니까요. **판단이 맥락에 따라 달라지는 거죠.**
>
> 그래서 대화를 '툴 쓰는 대화 / 안 쓰는 대화'로 나누지 않고 **한 대화 안에 둘을 섞었습니다.** 최종적으로 2,368개 대화, 평균 6.6턴인데 **그중 75%가 툴 호출과 즉답이 한 대화 안에 같이 들어있는 형태**예요. 질문 기준으로는 툴 호출 57%, 즉답 43% 정도 됩니다.
>
> 실무적으로 제일 까다로웠던 건 **포맷**이었습니다. 데이터는 OpenAI로 생성했는데, **Qwen의 툴 호출은 구조화된 필드가 아니라 텍스트**예요. assistant 응답 본문에 `<tool_call>` 태그로 JSON이 들어가고, 검색 결과는 tool role이 아니라 **user 턴에 `<tool_response>`로** 되돌아옵니다. 생성 결과를 이 템플릿에 맞춰 전부 재구성해야 했어요. 그리고 loss는 **assistant 응답에만** 걸었는데, user 턴이나 검색 결과에 loss를 주면 모델이 사용자 발화나 검색 결과를 흉내내게 되거든요.
>
> 결과적으로 tool_selection이 **79.22%에서 98.05%로** 올랐습니다."

**여기서 흘려야 할 키워드**: 툴 호출 정책 / 선택적 판단 / 맥락 의존 / 채팅 템플릿 / 라벨 마스킹

### 2-E. 클로징 (15초)

> "정리하면, **같은 문제인데 제어권이 어디 있느냐에 따라 완전히 다르게 풀어야 했던 것** — 그게 이 프로젝트에서 가장 어려웠고 가장 많이 배운 부분입니다.
> 혹시 어느 쪽을 더 자세히 말씀드리면 좋을까요?"

### 2-F. 짧게 끊어야 할 때

반응이 미지근하거나 시간이 없으면 2-A → 2-B까지만 하고 이렇게 끊습니다.

> "다른 하나는 사내 모델 파인튜닝 쪽인데, **툴 호출 판단 자체를 학습 데이터로 심은** 이야기입니다. 이어서 말씀드릴까요?"

### 2-G. 축별 꼬리질문

**축 1 (LangGraph)**

| 질문 | 답변 요지 |
|---|---|
| "품질 평가도 LLM이 하는데 그 판정을 믿을 수 있나요?" | 그래서 지표를 한 종류로 안 씀 — RAGAS(자동) + 자체 정량(LLM 심판) + TOOL CALL(규칙 기반) 3중. 평가 프롬프트는 **회피성 답변을 무조건 bad로** 잡도록 명시 |
| **"HyDE가 만든 가상 답변이 환각이면요?"** | 가상 답변은 **사용자에게 나가지 않습니다.** 검색 쿼리를 만드는 용도로만 쓰고, 최종 답변은 실제 검색된 문서를 근거로 다시 생성합니다 |
| "재시도 1회면 부족하지 않나요?" | 2회 이상은 응답 지연이 커져 실사용이 어려움. 대신 **재검색 시 Top-K를 15/30으로 키워** 한 번의 재시도 품질을 올리는 쪽을 택했습니다 |
| "정확도를 올린 대가는요?" | **응답 속도**입니다. 앙상블 검색 + 한·영 쿼리 + HyDE가 쌓이며 느려졌고, 태그별 BM25 인덱스 캐싱으로 일부 완화했습니다. 다음 단계는 시맨틱 캐시와 Cross-encoder 리랭킹입니다 |
| "왜 LangChain이 아니라 LangGraph죠?" | 조건 분기와 **되돌아가는 루프**가 필요했기 때문. 평가 결과에 따라 앞 노드로 돌아가는 구조는 체인으로 표현하기 어렵습니다 |

**축 2 (파인튜닝 데이터)**

| 질문 | 답변 요지 |
|---|---|
| **"그럼 학습 코드는 못 짜시는 건가요?"** | 처음엔 도움을 받았지만 지금은 설명할 수 있습니다 — **LoRA rank 8 / alpha 32, `q_proj`·`v_proj`만 타겟**으로 학습 파라미터를 줄이고, **bf16 + gradient checkpointing**으로 8B를 제한된 VRAM에 올렸습니다. max_len은 멀티턴이라 8192로 잡았습니다 |
| "OpenAI로 만든 데이터로 Qwen을 학습시키면 포맷이 다르지 않나요?" | 2-D의 포맷 문단 그대로. **이 질문이 나오면 기회입니다** |
| "일상 대화 데이터가 왜 필요하죠?" | 없으면 **과호출**. 모든 질문에 툴을 부릅니다. 즉답 43%를 확보하고 대화의 75%를 혼합 구성해 맥락에 따라 판단하도록 학습시켰습니다 |
| "왜 assistant 응답만 라벨링했나요?" | user 턴이나 검색 결과에 loss를 주면 모델이 **사용자 발화와 검색 결과를 모방**하게 됩니다 |
| "합성 데이터라 실제 분포와 다르지 않나요?" | **인정하는 게 정답.** 가장 약한 고리이므로 먼저 꺼내세요 — "실사용 로그 기반 데이터로 보강하는 게 다음 단계입니다" |
| "Qwen2.5가 99.68%로 더 높은데 왜 Qwen3죠?" | TOOL CALL 단일 지표만 우위. RAGAS(0.9273 vs 0.7697)와 자체 정량(70.30 vs 64.65)에서 전 지표 열세라 **RAG 전반 성능 기준**으로 택했습니다 |

---

<a id="3"></a>
## 3. ★ "테스트 및 품질관리" 답변 — 2층 구조

> ⚠️ 공고의 이 항목은 **코드 레벨 테스트(단위·통합·CI)** 를 뜻할 수도, **모델 출력 품질 검증**을 뜻할 수도 있습니다. 섞어 답하면 과장으로 읽히니 **반드시 층위를 나눠** 답하세요.

### 3-A. 메인 답변 (약 50초)

> "두 층위로 나눠서 말씀드리겠습니다.
>
> **런타임에서는**, 답변이 나가기 전에 LLM이 스스로 good/bad를 판정하고 bad면 재검색하는 **자가 교정 루프**를 넣었습니다. 특히 '죄송하지만 문서에 없습니다' 같은 **회피성 답변을 무조건 bad로 잡도록** 평가 프롬프트에 명시했는데, 이게 환각과 무응답을 거르는 핵심이었습니다.
>
> **개발 단계에서는**, 구조를 바꿀 때마다 **동일 데이터셋·동일 채점 모델로 버전 A/B**를 돌렸습니다. 총 6단계 버전을 기록했고, 지표는 성격이 다른 세 가지를 병행했어요. RAGAS로 검색·생성 품질을, 자체 정량 평가로 실사용 관점을, TOOL CALL은 규칙 기반으로 자동 채점했습니다.
>
> 이 체계가 실제로 값을 한 사례가 있는데, **Top-K를 늘리면 당연히 좋아질 거라고 생각했던 가정이 틀렸다는 걸** 여기서 잡았습니다. Recall이 0.8000에서 0.6750으로 떨어졌거든요. 평가 체계가 없었으면 그냥 넘어갔을 겁니다."

### 3-B. 코드 레벨 테스트를 물어올 때 — 경계 긋기

> ⚠️ **면접 전 확인**: 실제로 pytest 등 단위/통합 테스트를 작성했는지 스스로 점검하고, 아래 문장을 사실에 맞게 조정하세요. **여기서 뭉개면 신뢰를 잃습니다.**

> "솔직히 말씀드리면 **코드 레벨 테스트는 얕습니다.** 제가 구축한 건 **모델 출력 품질을 검증하는 체계**였고, 커버리지를 체계적으로 관리하진 못했습니다.
> 다만 **'바꾼 게 실제로 좋아졌는지를 매번 같은 기준으로 확인한다'** 는 원칙은 동일하다고 생각합니다. 정답이 없는 LLM 출력에도 재현 가능한 평가 기준을 만들어 본 경험이 있어서, 결정적인 결과가 나오는 코드 테스트 쪽은 더 빠르게 붙일 수 있다고 봅니다."

### 3-C. 꼬리질문

| 질문 | 답변 요지 |
|---|---|
| "평가를 자동화했나요?" | 평가셋과 채점 프롬프트를 고정해두고 버전마다 동일하게 재실행하는 방식. **CI에 물린 건 아니고 수동 트리거**였습니다 |
| "회귀는 어떻게 잡았나요?" | 버전별 지표를 표로 누적 기록해서 **떨어진 지표가 보이면 되돌리는** 방식. VER-3이 그 사례입니다 |
| "평가 표본이 20개면 적지 않나요?" | **인정.** 그래서 Top-K 선정 때는 조합 간 차이가 미미했던 RAGAS 대신 **자체 정량 평가를 기준**으로 삼았고, 소수점 차이는 과대 해석하지 않았습니다 |

---

<a id="4"></a>
## 4. 프롬프트 엔지니어링 보강

`INTERVIEW_SCRIPT.md`의 **3-H**는 그대로 쓰되(temperature 고정 / 회피성 답변 감지 / 오타 교정), **넷째 사례**를 추가하세요. 공고의 *프롬프트 엔지니어링* + *TOOL 연동*을 한 번에 칩니다.

> "넷째, 사내 sLLM은 **시스템 프롬프트에 `<tools>` 함수 시그니처를 JSON Schema로 주입**하고, **'일상적인 인사에는 툴을 부르지 말라'는 예외 규칙까지 명시**했습니다. 툴 호출 여부를 **프롬프트 레벨에서 먼저 정의하고, 그 정책대로 학습 데이터를 만든** 구조예요. 팀별로 주입하는 툴 시그니처를 다르게 해서, 프론트엔드 팀 계정은 애초에 백엔드 검색 툴이 프롬프트에 존재하지도 않게 했습니다."

*(실제 학습 데이터의 시스템 프롬프트에 그대로 들어있는 내용입니다 — 근거 있는 발언입니다.)*

---

<a id="5"></a>
## 5. 프로젝트 소개 보강 2줄

`INTERVIEW_SCRIPT.md` 1-B(1분 버전) 골격은 유지하고 두 문장만 얹습니다.

| 보강 | 넣을 문장 |
|---|---|
| **데이터 규모** | "구글 API 공식 문서 2,000여 건에서 **QA 14,858쌍**을 만들어 벡터 DB를 구성했고, sLLM 학습용으로는 **멀티턴 대화 2,368건**을 설계했습니다." |
| **평가 조건 선제 공개** | "Perplexity 비교는 **구글 API 질문 20문항을 GPT-4.1-mini로 채점**한 결과입니다." |

> 두 번째가 특히 중요합니다. +21.11%p를 조건 없이 말하면 "표본이 몇 개죠?"가 반드시 들어옵니다. **먼저 밝히면 방어가 아니라 신중함으로 읽힙니다.**

---

<a id="6"></a>
## 6. 이번 공고용 전달 원칙

| 원칙 | 내용 |
|---|---|
| **숫자는 축당 2개** | 축 1은 `0.6750 → 0.9250`, 축 2는 `98.05%`와 `혼합 대화 75%`. 나머지는 꼬리질문용으로 아낍니다 |
| **브릿지 문장을 빼지 말 것** | 2-C를 생략하면 그냥 두 개의 자랑이 됩니다. 이 한 줄이 답변의 값을 결정합니다 |
| **"에이전트"라는 단어를 의식적으로** | 공고 1순위 키워드. 계획 → 실행 → 관찰 → 자기 평가 → 재시도를 말할 때 반드시 붙입니다 |
| **파인튜닝을 "ML 이야기"로 말하지 말 것** | "학습 데이터 만들기"가 아니라 **"툴 호출 정책 설계"** 로 부릅니다. 같은 내용도 이름에 따라 공고 적합도가 갈립니다 |
| **한계는 먼저 꺼내기** | 속도 트레이드오프, 합성 평가셋, 20문항 표본. 스스로 말하면 나머지 수치의 신뢰도가 올라갑니다 |
| **축 1을 60초로 압축 연습** | 실전에서 가장 자주 무너지는 지점이 "두 번째 축이 급해지는 것"입니다. 소리 내서 연습하세요 |

---

<a id="7"></a>
## 7. 면접 전 반드시 확인할 것

- [ ] **코드 레벨 테스트를 실제로 작성했는지** — 3-B 문장을 사실에 맞게 조정 (과장 금지)
- [ ] **본인이 직접 한 백엔드 범위** — Django `apichat/utils` 통합은 본인 작업, 배포·CI는 팀원 담당임을 명확히 구분
- [ ] **배포 환경** — sLLM = RunPod GPU + vLLM, Django = Elastic Beanstalk + Docker (`INTERVIEW_QA.md` 기준)
- [ ] **README의 "업무:일상 2:1"** — 최종 데이터 실측은 툴 호출 57% / 즉답 43%. 면접에서는 **실측값**을 쓰세요
- [ ] LoRA 설정값을 **이유까지** 말할 수 있는지 (rank 8 / alpha 32 / q_proj·v_proj / bf16 / gradient checkpointing / max_len 8192)
