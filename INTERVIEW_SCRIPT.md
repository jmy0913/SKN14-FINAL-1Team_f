# CodeNova 면접 발화 대본 (구어체)

> 대상 직무: **LLM 서비스 엔지니어 (AI 백엔드 개발자)**
> 핵심 키워드: AI Agent · RAG · LangGraph · 프롬프트 튜닝 · RAG 평가 프로세스 · Python 백엔드 · 클라우드
> 담당: 정민영 — ① API 챗봇 LangGraph 설계 ② sLLM(Qwen3-8B) LoRA 파인튜닝 ③ LLM/sLLM 성능 평가
> 상세 근거 Q&A는 `INTERVIEW_QA.md`(Q1~Q41) 참고.

---

## 0. 목차
1. 프로젝트 소개 — 30초 / 1분 / 클로징 버전
2. LangGraph 구조 설명 + 꼬리질문 (+ ★ AI Agent 스토리)
3. AI / RAG / LLM 꼬리질문
4. 성능 평가 꼬리질문
5. 직무 맞춤 전달 포인트
6. 전달 팁 (말하기 요령)

---

## 1. 프로젝트 소개

### 1-A. 30초 핵심 (엘리베이터 피치)

> "**CodeNova**라는 프로젝트인데, 한마디로 **개발자를 위한 '근거 기반' 문서 검색 AI 시스템**입니다.
> 구글 API 공식 문서와 사내 내부 문서를 근거로, **출처가 인용된 답변과 바로 실행 가능한 예제 코드**를 주는 게 핵심이에요.
>
> 챗봇이 두 개로 나뉘는데, 하나는 **GPT-4o 기반에 LangGraph로 오케스트레이션한 외부 API 어시스턴트**고, 다른 하나는 **Qwen3-8B를 LoRA로 직접 파인튜닝해서 vLLM으로 서빙한 사내 전용 챗봇**입니다.
>
> 저는 이 중에서 **① API 챗봇의 LangGraph 파이프라인 설계, ② sLLM 파인튜닝, ③ 두 모델의 성능 평가**를 맡았습니다."

### 1-B. 1분 핵심 버전 (가장 자주 쓸 버전)

> "**CodeNova**라는 프로젝트입니다. 한마디로 **개발자를 위한 근거 기반 문서 검색 AI 시스템**인데요, 구글 API 공식 문서와 사내 내부 문서를 근거로 **출처가 인용된 답변과 실행 가능한 예제 코드**를 주는 게 목표였습니다.
>
> 챗봇은 용도에 따라 두 개로 나눴습니다. 하나는 **외부 구글 API 어시스턴트**로, 멀티모달과 구조화 출력이 필요해서 **GPT-4o 기반에 LangGraph로 전체 워크플로우를 설계**했습니다. 질문을 먼저 3분류하고, 하이브리드 RAG로 검색한 뒤, **답변 품질을 LLM이 스스로 평가해서 나쁘면 HyDE 방식으로 재검색하는 자가 교정 루프**가 핵심입니다.
>
> 다른 하나는 **사내 기밀 문서 전용 챗봇**인데, 외부 API로 본문을 보낼 수 없어서 **Qwen3-8B를 LoRA로 직접 파인튜닝**하고 vLLM으로 서빙했습니다. 팀·직급별로 벡터 DB와 도구를 분리해 **권한 기반 접근 제어**도 넣었고요.
>
> 제가 맡은 부분은 **API 챗봇의 LangGraph 설계, sLLM 파인튜닝, 그리고 두 모델의 성능 평가** 세 가지입니다. 평가는 '감이 아니라 데이터로 증명한다'는 원칙으로 진행했고, 대표적으로 **파인튜닝으로 TOOL CALL 정확도를 79%에서 98%까지** 끌어올렸습니다."

### 1-C. 클로징 멘트 (꼬리질문 유도)

> "혹시 **LangGraph의 자가 교정 루프**, **sLLM 파인튜닝 데이터 설계**, **성능 평가 방법론** 중에 어떤 부분을 더 자세히 말씀드리면 좋을까요?"

---

## 2. LangGraph 구조 설명

### 2-A. "LangGraph 구조가 어떻게 되나요?" — 메인 답변 (약 1분 20초)

> "네, 제가 직접 설계한 부분이라 흐름대로 말씀드릴게요.
>
> 일단 LangGraph를 쓴 이유부터 말씀드리면, RAG가 단순히 '검색해서 답하기' 한 방으로 끝나는 게 아니라 **분기도 있고 재시도 루프도 있어서**, 이걸 if문으로 떡칠하는 것보다 **노드와 엣지로 흐름을 명시적으로 그리는 게** 훨씬 관리하기 좋았거든요. 그래서 상태를 `ChatState`라는 걸로 들고 다니면서 노드를 거치는 구조로 만들었습니다.
>
> 흐름을 따라가면요, 제일 앞에 **이미지 분석 노드**가 있어요. 이미지가 들어오면 GPT-4o 비전으로 텍스트 설명으로 바꿔주고, 없으면 그냥 통과합니다.
>
> 그다음이 핵심인데 **분류 노드**예요. 질문을 `api`, `basic`, `none` 세 가지로 나눕니다. 구글 API나 기술 질문이면 `api`, 그냥 인사 같은 일상 대화면 `basic`, 양자컴퓨터처럼 우리 서비스 범위 밖이면 `none`. 이렇게 먼저 거르는 이유는, **모든 질문을 RAG로 보내면 비용·지연도 낭비고 엉뚱한 문서를 끌어와서 환각이 생기거든요.** basic은 가벼운 모델로 바로 답하고, none은 정중히 거절하고 끝냅니다.
>
> `api`로 분류되면 본 파이프라인을 타는데, **질문 통합 → 쿼리 분리 → 검색 → 답변 → 평가** 순서예요.
> - 먼저 **최근 대화 4개랑 이미지 설명, 현재 질문을 하나로 통합**합니다. 이게 멀티턴에서 중요한데, '그럼 그건 어떻게 해?' 같은 대명사를 풀어주는 단계예요.
> - 그다음 그 통합된 질문을 **JSON 모드로 한글·영어 여러 쿼리로 쪼갭니다.** 다국어로 검색해야 리콜이 올라가서요.
> - 그리고 **툴 노드**에서 LLM이 쿼리마다 '이건 드라이브 API다, 이건 캘린더다' 하고 **알맞은 태그를 스스로 골라서** 하이브리드 검색을 호출합니다.
> - 검색 결과로 답변을 생성하고요.
>
> 마지막이 제가 제일 신경 쓴 **평가 노드**입니다. 생성된 답변을 LLM이 good이냐 bad냐 스스로 판정해요. good이면 끝, **bad면 HyDE 방식으로 가상 답변을 만들어서 다시 검색하고 재답변**합니다. 일종의 자가 교정 루프죠.
> 대신 **무한 루프 방지로 재시도는 딱 한 번**으로 막아놨어요. 한 번 재시도했는데도 bad면 그냥 종료시킵니다."

**흐름 요약 (화이트보드용)**
```
analyze_image → classify ─┬─(api)──→ extract_queries → split_queries → tool → basic → evaluate
                          ├─(basic)→ simple → END
                          └─(none)─→ impossible → END
evaluate ─┬─(good/final)→ END
          └─(bad)───────→ generate_queries(HyDE) → tool → basic → evaluate  (재시도 최대 1회)
```

### 2-B. "왜 LangChain만으로 안 하고 LangGraph를 썼어요?"

> "체인만으로도 선형 RAG는 되는데, 저희는 **'평가하고 나쁘면 되돌아가서 다시 검색'하는 사이클**이 있었거든요. 체인은 본질적으로 한 방향이라 이런 루프를 표현하기가 어색해요. LangGraph는 **조건부 엣지로 '평가 결과에 따라 종료할지 재검색 노드로 돌아갈지'를 그래프에 그대로 그릴 수 있어서** 훨씬 직관적이었습니다. 상태도 체크포인터로 세션별로 관리되니까 멀티턴도 자연스럽게 붙었고요."

### 2-C. "상태(State)는 어떻게 관리했어요?"

> "`ChatState`라는 TypedDict로 만들었는데, `total=False`로 뒀어요. **노드마다 채우는 필드가 다르거든요.** 예를 들어 basic이나 none 분기는 검색을 안 하니까 검색 결과 필드를 아예 안 채웁니다. 그래서 모든 필드를 필수로 두면 안 맞아서, 선택적으로 채워지게 했어요. 안에는 질문, 답변, 통합 질문, 분리된 쿼리들, 원문·QA 검색 결과, 히스토리, 재시도 플래그 같은 게 들어 있습니다."

### 2-D. "멀티턴 맥락은 어떻게 유지해요?"

> "두 단계로 했어요. 하나는 **LangGraph 체크포인터(MemorySaver)로 세션 ID 단위 상태를 유지**하고, 다른 하나는 **거의 모든 노드에서 최근 메시지 4개만 윈도잉**해서 씁니다. 전체 히스토리를 다 넣으면 토큰 비용이랑 지연이 커지니까, 4개로 제한하되 그 안에서 대명사 해소·맥락 통합은 충분히 되도록 했어요."

### 2-E. "분류 노드가 틀리면 전체가 망가지지 않나요?" *(압박)*

> "맞아요, 그게 단일 분류 의존의 리스크죠. 솔직한 약점입니다. 그래서 완화를 두 가지 했는데, 하나는 **프롬프트에 경계 케이스 예시를 많이 넣어서** 정확도를 올렸어요. 예를 들어 '구글 맵 API 알려주고 참고로 배고파'는 api로 분류하라는 식으로 혼합 질문 규칙까지 명시했습니다.
> 더 견고하게 하려면 **분류 신뢰도가 낮을 때 안전하게 api 쪽으로 폴백**시키는 방향을 생각하고 있어요. 잘못 basic으로 빠지는 것보다 RAG 한 번 더 타는 게 나으니까요."

### 2-F. ★ AI Agent 스토리 ("에이전트 구축 경험 있나요?" / "자랑할 만한 기능은?")

> *이 직무의 우대사항이 'AI Agent 구축 경험'이라, 이 스토리를 의식적으로 꺼내세요.*

> "제가 가장 에이전트답게 설계했다고 생각하는 부분은 **검색 단계예요.** 보통 RAG는 사용자 질문을 그대로 임베딩해서 벡터 검색 한 방 하잖아요. 저는 거기서 한 단계 더 나아가서, **LLM이 스스로 '어떤 도구를, 어떤 인자로 부를지'를 판단하게** 만들었습니다.
>
> 구체적으로 말씀드리면, 저희는 구글 API가 11개라 문서를 API별 태그로 관리하는데요. 사용자가 '이거 드라이브 API야'라고 안 알려줘도, **LLM한테 선택 가능한 태그 목록이랑 검색 툴을 바인딩해서 주고, 쿼리마다 알맞은 태그를 골라 `vector_search_tool`을 호출하라**고 시킵니다. 그러면 모델이 `{query: ..., api_tags: ['drive']}` 이런 식으로 **툴 콜을 직접 생성**해요. 고른 태그는 그대로 벡터 DB 메타 필터로 들어가서 검색 공간을 그 API로 좁히고요. 이게 단순 임베딩 검색 대비 정밀도를 크게 올린 핵심이었습니다.
>
> 그리고 이게 한 번으로 안 끝나요. 답변을 만든 다음에 **품질 평가 노드가 'good이냐 bad냐'를 스스로 판정하고, bad면 HyDE로 가상 답변을 만들어 다시 검색·재답변**합니다. 즉 **'계획 → 도구 실행 → 결과 관찰 → 자기 평가 → 재시도'라는 에이전트 루프**가 도는 거죠. 무한 루프는 막아야 하니까 재시도는 최대 1회로 제한했고요.
>
> 사내 sLLM 챗봇은 더 본격적인 에이전트인데, **파인튜닝으로 '언제 도구를 부르고 언제 안 부르는지'를 모델한테 직접 학습**시켰어요. 일상 질문엔 툴 없이 즉답하고, 문서가 필요한 질문엔 `tool_call`을 뱉도록요. 서빙 쪽에선 그 tool_call을 파싱해서 **권한에 맞는 도구만 실행 → 결과를 다시 넣고 → 최종 답변 생성**하는 멀티스텝으로 돌립니다. 권한별로 부를 수 있는 도구 자체를 제한해서, 타 팀 문서엔 애초에 접근이 안 되게 한 것도 에이전트 설계에서 신경 쓴 부분이에요."

**꼬리 — "그게 그냥 function calling이랑 뭐가 달라요?"**

> "기술적으론 function calling 패턴이 맞아요. 다만 제가 강조하고 싶은 건 **'에이전트가 자율적으로 판단하는 범위를 어디까지 줄 거냐'를 설계한 부분**입니다. 태그 선택은 LLM한테 자율권을 줘서 정밀도를 얻었고, 반대로 **권한 제어 같은 보안은 절대 LLM 판단에 안 맡기고 코드 계층에서 결정론적으로** 막았어요. '어디는 모델을 믿고 어디는 못 믿는지'를 나눈 게 핵심입니다."

**꼬리 — "에이전트가 잘못 판단하면요?"**

> "두 가지 안전장치가 있어요. 하나는 **품질 평가 노드가 결과를 보고 bad면 되돌리는** 자기 교정이고, 다른 하나는 **재시도 횟수 제한**으로 폭주를 막는 거예요. 그리고 보안 관련 판단은 아예 모델한테 안 줬으니까, 모델이 헛짚어도 권한 밖 문서는 못 봅니다. **자율성과 통제를 분리한 게** 잘못된 판단의 영향을 가두는 방식이었습니다."

---

## 3. AI / RAG / LLM 꼬리질문

### 3-A. "하이브리드 검색을 어떻게 구성했나요?"

> "**Dense와 Sparse를 RRF로 결합**했습니다. Dense는 Chroma에 BGE-m3 임베딩, Sparse는 BM25를 쓰고, LangChain `EnsembleRetriever`로 **가중치 0.8 대 0.2**로 묶었습니다.
> Dense를 주력으로 하되 BM25를 섞은 이유는, API 문서엔 `projects.databases.operations.get` 같은 **정확한 토큰 매칭이 중요한 식별자**가 많아서 의미 검색만으로는 놓치기 때문입니다."

**꼬리 — "EnsembleRetriever가 점수를 가중평균하나요?"** *(여기서 깊이가 갈림)*

> "흔히 오해하는데 **점수 평균이 아니라 RRF, 역순위 융합**입니다. Dense 코사인 유사도는 0~1이고 BM25 점수는 0에서 수십까지라 **스케일이 달라 직접 더할 수 없거든요**. 그래서 점수 대신 순위로 정규화해서, `weight × 1/(rank + 60)`을 더합니다. 그래서 0.8/0.2는 '점수를 4배'가 아니라 **'Dense의 순위 신호를 4배 신뢰한다'**는 의미입니다."

### 3-B. "왜 원문 DB랑 QA DB를 둘 다 뒀나요?"

> "실험으로 정했습니다. **원문 DB는 정답 일치율은 높은데 Recall이 낮고**, **QA DB는 Recall은 좋은데 정답 일치율이 낮았습니다.** 두 구조가 잘 푸는 질문 유형이 상호 보완적이어서, **둘 다 검색해서 답변 프롬프트에 함께 넣으니** Recall이 0.80으로 가장 균형이 좋았습니다."

### 3-C. "검색 태그를 LLM이 자동 선택한다는 게 무슨 의미예요?"

> "구글 API가 11개라 메타데이터 태그로 관리하는데, 사용자가 '이거 드라이브 API야'라고 안 알려줘도 돼요. **툴 노드에서 LLM한테 선택 가능한 태그 목록을 주고, 쿼리마다 알맞은 태그를 골라서 검색 툴을 호출하라고** 시킵니다. 고른 태그는 Chroma 메타 필터로 들어가서 **검색 공간을 그 API로 좁혀** 노이즈를 줄여요. 이게 그냥 임베딩 검색 대비 정밀도를 올린 핵심입니다. AI Agent적인 동작이기도 하고요."

### 3-D. "답변 품질이 나쁠 때 재검색은 어떻게 하나요? (HyDE)"

> "**HyDE, Hypothetical Document Embeddings** 방식입니다. 품질 평가가 bad로 나오면, 원래 질문을 다시 검색하는 게 아니라 **LLM이 자기 지식으로 '가상의 답변'을 한글·영어로 생성**하고, 그걸 새 검색 쿼리로 씁니다.
> 가상 답변은 질문 문장보다 **실제 정답 문서와 임베딩 공간에서 더 가깝기 때문에** 리콜이 올라갑니다. 실제로 이 구조가 **Faithfulness를 0.69에서 0.88로** 끌어올린 결정적 버전이었습니다."

**꼬리 — "재시도가 무한 루프 안 도나요?"**

> "**재시도는 최대 1회로 못박았습니다.** 평가 노드에서 이미 retry 플래그가 True인데 또 bad가 나오면 강제로 종료시키고, 재검색 노드도 진입 시점에 재진입을 막습니다. 비용 폭주와 무한 루프를 동시에 차단한 거죠. 그리고 재검색 때는 Top-K를 초기보다 넓혀서(원문 5→15, QA 20→30) 봅니다. 한 번 실패했다는 건 정답이 상위 K에 없었을 가능성이 크니까요."

### 3-E. "왜 외부 API 말고 sLLM을 직접 파인튜닝했나요?"

> "**사내 기밀 문서를 외부 API로 본문째 보낼 수 없었고**, 사내 말투·보고체계·TOOL CALL 패턴을 학습시켜야 했습니다. 그래서 자체 호스팅 가능한 **Qwen3-8B를 LoRA로 파인튜닝**했습니다.
> 풀 파인튜닝 대신 LoRA를 쓴 건 8B 풀튜닝은 VRAM·시간 비용이 크고 도메인 적응엔 어댑터로 충분했기 때문입니다. **rank=8, alpha=32, q/v projection 타깃**으로 잡았습니다."

**꼬리 — "데이터에서 가장 신경 쓴 부분은?"**

> "**'언제 툴을 부르고 언제 안 부르는지'를 멀티턴 맥락에서 판단**하도록 학습시킨 겁니다. 문서 검색 질문은 `질문 → tool_call → tool_response → 답변` 형태로, 일상 질문은 툴 없이 즉답하도록 라벨링했고요. 그리고 **assistant 응답 토큰만 loss에 포함**했습니다. 사용자 질문까지 학습하면 모델이 '질문 생성법'을 배워버려서요."

### 3-F. "tool call을 function calling으로 안 하고 직접 파싱한 이유는?"

> "파인튜닝된 Qwen이 답변 본문에 **`<tool_call>{...}</tool_call>` XML 형태**로 도구 호출을 내보내거든요. 파인튜닝 데이터 자체를 Qwen 네이티브 포맷으로 만들었기 때문에, **서빙에서도 정규식으로 그 포맷을 직접 파싱**해서 모델 출력이랑 일치시켰습니다. LLM 1차 호출로 tool_call 뽑고 → 함수 실행 → 결과를 다시 넣고 → 2차 호출로 최종 답변, 이런 흐름이에요. Qwen3는 `<think>` 추론 태그도 뱉어서 그건 사용자에게 안 보이게 제거합니다."

### 3-G. "권한 제어는 어떻게 했나요?" *(보안)*

> "**프롬프트가 아니라 실행 계층에서 막았습니다.** 두 단계인데, ① 팀별로 Chroma DB를 물리적으로 분리하고, ② permission 값에 따라 **그 사용자에게 허용된 검색 툴만 모델에 바인딩**합니다. 프론트엔드 사용자에겐 백엔드 검색 도구가 애초에 노출되지 않아요.
> 프롬프트 가드는 인젝션이나 모델 오판으로 뚫릴 수 있어서, **보안을 LLM 판단에 의존시키지 않은 게** 핵심입니다."

### 3-H. "프롬프트 엔지니어링은 어떻게 했어요?"

> "노드별로 역할이 달라서 프롬프트도 다 따로 튜닝했어요.
> 첫째, **분류·검색·평가처럼 일관성이 중요한 노드는 temperature를 0으로** 고정해 재현성을 확보했고요.
> 둘째, **평가 프롬프트가 가장 공들였어요.** 단순히 맞냐 틀리냐가 아니라, **'죄송하지만 문서에 없습니다' 같은 회피성 답변을 무조건 bad로 잡도록** 명시했어요. 이게 환각이랑 무응답을 거르는 핵심이었습니다.
> 셋째, 쿼리 분리 프롬프트엔 **오타 교정 규칙**까지 넣었어요. 사용자가 API 메서드명을 틀리게 쳐도 정정해서 검색하도록요."

### 3-I. "멀티모달은 어떻게 처리했어요?"

> "이미지랑 음성을 모두 **텍스트로 정규화해서 같은 파이프라인을 태우는** 구조예요. 이미지는 GPT-4o 비전으로 설명 텍스트를 만들고, 음성은 Whisper로 STT 합니다. 그래서 분류·검색·답변 노드는 입력이 뭐였든 똑같이 동작해요. 이미지 원본은 S3에 저장해서 URL로 관리하고, 분석 텍스트는 따로 보관해서 '아까 그 이미지 뭐였지?' 같은 후속 질문에도 재사용합니다."

---

## 4. 성능 평가 꼬리질문

### 4-A. "어떤 지표로 평가했고 왜 여러 개를 썼나요?"

> "**세 축**으로 봤습니다. **RAGAS**로 context_recall·faithfulness·factual_correctness를 보고, **자체 정량 RAG 평가**로 정확도·재현율·구체성을 LLM-as-judge로 채점하고, **TOOL CALL 평가**로 도구 선택·파라미터 정확도를 봤습니다.
> RAGAS만으로 부족했던 건, **factual_correctness가 표면적 표현 차이에 민감해서** 실제 품질을 과소평가하는 경우가 있었기 때문입니다. 그래서 자체 척도를 병행해 상호 검증했습니다."

### 4-B. "파인튜닝 효과를 숫자로 말해줄 수 있나요?"

> "네. Qwen3-8B 기준 **TOOL CALL 도구 선택 정확도가 79.2%에서 98.1%로 +18.8%p**, 파라미터 값 유사도가 77.2%에서 88.5%로 올랐습니다. 특히 Qwen2.5-7B는 베이스가 26%로 툴을 거의 못 불렀는데 파인튜닝 후 99.7%까지 갔고요. **Qwen 특유의 `<tool_call>` XML 포맷을 못 따르던 걸 포맷과 판단을 함께 학습시켜 교정**한 결과입니다."

**꼬리 — "그럼 왜 더 높은 Qwen2.5-7B 말고 Qwen3-8B를 최종 선택했나요?"** *(함정)*

> "**TOOL CALL은 둘이 비등했지만, 실제 답변 품질에서 Qwen3-8B가 앞섰습니다.** RAGAS에서 context_recall이 0.93 대 0.77, faithfulness가 0.86 대 0.80이었어요. **'도구를 잘 부르는 것'과 '검색 결과로 좋은 답을 쓰는 것'은 다른 능력**이고, 사용자 경험엔 후자가 직결되기 때문에 Qwen3-8B를 골랐습니다."

### 4-C. "Top-K 같은 하이퍼파라미터는 어떻게 정했나요?"

> "**데이터로 정했습니다.** sLLM은 k를 4부터 8까지 돌려서 **k=7이 평균 72.5점으로 최고**라 고정했고, API 챗봇은 재검색 시 (원문, QA) K 조합을 실험해서 **초기 5/20에서 재검색 때 15/30으로 늘리는 게** 총점 18.35/20으로 품질·속도 균형이 가장 좋아 채택했습니다.
> 처음에 좁게 잡는 건 비용·속도 때문이고, **한 번 실패했다는 건 정답이 상위 K에 없었을 가능성이 크니** 재검색 때만 넓히는 구조입니다."

### 4-D. "RAG 평가 프로세스를 어떻게 구축했나요?" *(직무 핵심)*

> "단발성 측정이 아니라 **버전별로 개선하면서 매번 같은 데이터셋으로 재평가하는 프로세스**를 만들었어요. API 챗봇은 VER-1부터 VER-6까지 구조를 바꿔가며 RAGAS를 돌렸는데, 예를 들어 **HyDE 재검색을 넣은 VER-4에서 Faithfulness가 0.69에서 0.88로** 뛰는 걸 확인하고 채택했고, 최종 VER-6은 한·영 쿼리 + HyDE + 원문/QA 앙상블로 Context Recall 0.925까지 올렸습니다. **'바꾼다 → 같은 셋으로 잰다 → 좋아졌으면 채택'**을 반복한 게 평가 프로세스의 핵심이었습니다."

### 4-E. "params_value_similarity는 어떻게 계산했어요?"

> "단순 문자열 일치로는 '구글 드라이브 파일 권한'이랑 '드라이브 권한 수정'을 다르게 보는데 의미는 거의 같잖아요. 그래서 **형태소 단위 Jaccard 0.6 + 문자 유사도 0.4의 가중 평균**으로 계산했어요. Jaccard로 의미 겹침을, 문자 유사도로 표기 차이를 잡아서 **부분 정답을 공정하게 평가**하려 한 겁니다."

### 4-F. "경쟁 서비스랑 비교는 해봤나요?"

> "**Perplexity와 동일 질문셋으로 정량 비교**했습니다. 정확도·재현율·신뢰성 전체 평균 **+21.1%p 우위**였고, 특히 신뢰성 격차가 +28%p로 컸습니다. 이건 우리 시스템이 **한정된 근거 안에서만 답하고, 회피성 답변을 평가 단계에서 걸러내서** 환각이 적었던 결과로 해석합니다."

---

## 5. 직무 맞춤 전달 포인트 (LLM 서비스 엔지니어)

| 직무 키워드 | 강조할 내 경험 |
|---|---|
| **AI Agent** | "LLM이 태그를 스스로 골라 툴을 호출" + "tool_call → 실행 → 재호출" 멀티스텝 = 에이전트적 동작. 이 단어를 의식적으로 쓰기 |
| **RAG** | 하이브리드 검색 + 원문/QA 이중 DB + HyDE 재검색 → "단순 RAG가 아니라 자가 교정 RAG" |
| **프롬프트 튜닝** | 노드별 프롬프트 분리, 회피성 답변 감지, temperature 전략, 오타 교정 |
| **RAG 평가 프로세스** | RAGAS + 자체 정량 + TOOL CALL + 버전별 A/B(VER-1~6) → *프로세스를 구축*한 경험으로 포장 (4-D가 핵심) |
| **Python 백엔드** | Django + FastAPI 이중 구조, 동기/비동기 호출 고민, vLLM 어댑터 계층 |
| **LangChain/LangGraph** | 실무 설계 경험 직접 보유 — 가장 강한 무기 |
| **클라우드 (AWS/Azure)** | 웹(Django/Docker)과 GPU 추론(vLLM) 분리 배포 → 독립 스케일링 |

### 배포 관련 답변 (클라우드 우대사항)

> "구조를 두 덩어리로 나눴어요. **Django 웹 서버는 Docker로 올리고**, GPU가 필요한 sLLM 추론은 분리했습니다. vLLM 추론은 GPU 위에서 돌아야 하는데 웹 서버는 CPU 환경이라, **sLLM을 별도 FastAPI 서비스로 띄워서 Django가 HTTP로 호출**하는 구조예요. 이렇게 하면 **모델 교체나 스케일링을 웹이랑 독립적으로** 할 수 있어서, GPU 서빙 계층을 떼어낸 게 운영 관점에서 좋았습니다."

> ⚠️ **면접 전 확인**: 실제 배포 환경(AWS인지, sLLM은 RunPod GPU인지)을 본인이 한 번 더 점검하세요. `INTERVIEW_QA.md`엔 sLLM = RunPod GPU + vLLM, Django = EB/Docker로 정리돼 있습니다.

---

## 6. 전달 팁 (말하기 요령)

| 상황 | 대응 |
|---|---|
| 프로젝트 첫 소개 | **"두 챗봇 / 두 모델 전략"을 먼저** 깔기 — "왜 GPT랑 sLLM 둘 다?"라는 흔한 첫 꼬리질문을 미리 차단 |
| 첫 답변에 숫자 | **딱 2개만** (TOOL CALL 98%, Perplexity +21%p). 디테일은 꼬리질문에서 |
| 내 역할 | "팀플 했어요"가 아니라 **"제가 맡은 건 ①②③"** 명확히 끊어 말하기 |
| RRF 질문 | **"점수 평균이 아니라 순위 융합"** 한 문장만 정확히 — 여기서 깊이가 드러남 |
| 숫자 답변 | "올랐어요"가 아니라 **"79에서 98로, +18.8%p"**까지 |
| 함정 질문 (왜 더 높은 모델 안 썼냐) | 당황 말고 **"두 능력은 다르다"** 관점으로 전환 |
| 한계 질문 | 솔직하게 + **개선안**까지 (리랭커 도입, 시맨틱 캐시 등). `INTERVIEW_QA.md` Q36 참고 |
| 용어(HyDE/LoRA/RRF) | 한 번씩만 던지고, 물으면 깊이 보여주기. 안 물으면 넘어가기 (나열 X) |
